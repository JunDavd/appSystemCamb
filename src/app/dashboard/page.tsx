const Dashboard = () => {
    return <div>Dashboard</div>
}

export default Dashboard

/*
 *<button
                    className={
                        'px-4 py-3 bg-gray-100 rounded-full hover:bg-blue-100'
                    }
                    onClick={() => {}}
                >
                    <Menu className={'w-3 h-4'} />
                </button>
 *
 *
 * */
